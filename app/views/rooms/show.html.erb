<script>

</script>
<h1>Room</h1>

ID: <%= @room.id %></br>
Name: <%= @room.name %></br>
<div>
  <div>
    <%= link_to(:highest_rated, highest_rated_questions_path(room_id: @room.id), :remote => true) %> | 
    <%= link_to(:most_recent, most_recent_questions_path(room_id: @room.id), :remote => true) %>    
  </div>
  
  <%= render 'questions/form' %>
  <%= render :partial => 'questions/questions', :locals => {:questions => @questions} %>
</div>